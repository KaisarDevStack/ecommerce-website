**1) What is the difference between null and undefined?**

 <br/>
ans: In Javascript, null means an intentional absence of value in a variable and undefined meaning of assigning a value in a variable.
 <br/>

 
**2) What is the use of the map() function in JavaScript? How is it different from forEach()?**
<br/>
ans: In javascript both map and forEach function is used to loop through the array. But map() function return a new array and otherway don't return new array. ForEach method is return undefine and change the previous function.

<br/>
**3) What is the difference between == and ===?**
ans: In JS, == is used to check value only and === is used to check type and value eventually.

<br/>
**4) Explain the concept of Scope in JavaScript (Global, Function, Block).**
ans: In JS global scope variable can be access from anywhere in the code, function scope can authorized to use within the function perimeter and block is mean { } within this scope it cann't be used.


